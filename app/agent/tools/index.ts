/**
 * 工具集合导出文件
 * 将所有工具统一导出，方便在配置文件中使用
 */

export { calculatorTool as calculatorConfig } from './calculator.tool';
export { weatherTool as weatherConfig } from './weather.tool';
export { currentTimeTool as currentTimeConfig } from './current-time.tool';
export { searchTool as searchConfig } from './search.tool';
